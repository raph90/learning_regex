/**
 * SECTION Metacharacters
 *
 * NOTE 1: Wildcard: .
 * The . represents any single character.
 * For example, /h.t/ will match hot AND hat.
 * This is also the case inside words.
 * h t will also match.
 * h@t will match.
 * hoot will NOT match. The wildcard is for single characters.
 * It will also not match a newline character, so h\nt won't match.
 * tab will match.
 *
 * ! to use a period, yo
 */